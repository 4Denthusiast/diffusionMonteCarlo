[profile.release]
debug = "line-tables-only"

[package]
name = "diffusion_monte_carlo"
version = "0.1.0"
edition = "2024"

[dependencies]
crossterm = "0.29.0"
rand = "0.10.0"
rand_distr = "0.6.0"
regex = "1.12.3"
